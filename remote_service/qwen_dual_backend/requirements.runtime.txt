# Added to the persistent venv. Dependencies are supplied by the verified vendor
# Python environment so bootstrap can use --no-deps without replacing torch.
diffusers==0.39.0
accelerate==1.14.0
python-multipart==0.0.32
httpx==0.28.1
pytest==9.1.1
